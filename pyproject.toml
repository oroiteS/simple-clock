[project]
name = "clock"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "apscheduler>=3.11.0",
    "flask>=3.1.2",
    "flask-cors>=6.0.1",
    "win10toast>=0.9",
]
